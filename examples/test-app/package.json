{
  "name": "test-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "start": "npm run clean && npm run build && node dist/index.js",
    "start:dry-run": "TWILIO_PKCV_EXAMPLE_DRY_RUN=true npm run start"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "fern-twilio-pkcv-sdk": "file:../../sdks/typescript"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
